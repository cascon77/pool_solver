import 'package:flutter/material.dart';

class AppColors {
  // Primary & Accents
  static const primaryDark = Color(0xFF0A2540);
  static const primaryAccent = Color(0xFF00B4D8);
  static const primaryAccentDark = Color(0xFF0096B4);

  // States
  static const success = Color(0xFF06D6A0);
  static const warning = Color(0xFFFFD166);
  static const danger = Color(0xFFEF4747);

  // Light Theme
  static const backgroundLight = Color(0xFFF8FAFC);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF0A2540);
  static const textSecondary = Color(0xFF64748B);

  // Dark Theme - Modern Deep Blue/Slate
  static const backgroundDark = Color(0xFF0F172A); // Slate 950
  static const surfaceDark = Color(0xFF1E293B);    // Slate 800
  static const surfaceDarkVariant = Color(0xFF334155); // Slate 700
  
  // Dark Theme Text
  static const textPrimaryDark = Color(0xFFF1F5F9);   // Slate 100
  static const textSecondaryDark = Color(0xFF94A3B8); // Slate 400

  // Borders
  static const borderLight = Color(0xFFE2E8F0);
  static const borderDark = Color(0xFF334155);
}
