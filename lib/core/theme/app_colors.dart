import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primaryDark = Color(0xFF0A2540);
  static const primaryAccent = Color(0xFF00B4D8);

  // States
  static const success = Color(0xFF06D6A0);
  static const warning = Color(0xFFFFD166);
  static const danger = Color(0xFFEF4747);

  // Light
  static const surfaceLight = Color(0xFFEFF9FC);
  static const backgroundLight = Color(0xFFFFFFFF);

  // Dark
  static const backgroundDark = Color(0xFF0D1F2D);
  static const surfaceDark = Color(0xFF132A3A);

  // Text
  static const textPrimary = Color(0xFF0A2540);
  static const textSecondary = Color(0xFF6B7C93);
}