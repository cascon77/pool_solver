import 'package:flutter/material.dart';

class PoolSetupScreen extends StatelessWidget {
  const PoolSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
